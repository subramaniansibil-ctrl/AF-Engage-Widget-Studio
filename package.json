{
  "name": "af-engage-widget-studio",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && go run ./cmd/server",
    "build": "npm --prefix frontend run build",
    "test:backend": "cd backend && go test ./..."
  }
}
